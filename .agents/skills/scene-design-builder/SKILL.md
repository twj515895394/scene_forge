---
name: scene-design-builder
description: 当用户要为 SceneForge 项目生成角色与场景的动画电影化设定，并根据制作档位产出轻量锁定卡或完整设定时应使用此技能。
---

# scene-design-builder

负责角色与场景的动画电影化设定。根据制作档位和资产判断结果，决定输出轻量锁定卡还是完整设定，并把较长设计内容写入 `details/`。

## 何时使用

在以下场景使用此技能：

- 总控 Skill 发现项目 `next_stage` 为 `scene-design-builder`
- 已完成资产复用判断，需要为角色和场景生成本次项目可执行的视觉设定
- 需要把参考强度、动画电影化风格和资产复用结论合并成统一设计结果

如果资产命中判断还没完成，或已经进入剧本改编，不要优先使用本技能。

## 执行步骤

1. 读取项目 `PROJECT_BOARD.md`，确认 `project_status` 已到 `assets_checked`。
2. 读取 `scene-reference-decider` 的边界和 `scene-asset-checker` 的复用结论。
3. 读取 `references/output-contract.md`，确认制作档位、参考强度和输出落点。
4. 判断本次走“轻量锁定卡”还是“完整设定”路径。
5. 为角色和场景分别产出统一的设计摘要、风格约束和复用说明。
6. 对较长的角色/场景设定正文，写入 `details/`；黑板里只保留摘要和引用路径。
7. 输出单个 YAML 补丁块，并给出下一阶段的剧本改编注意事项。
8. 将状态推进建议交回总控 Skill，进入 `scene-script-adapter`。

## 关键规则

- 重点制作池优先产出完整角色设定和完整场景设定
- 快速制作池优先产出轻量角色锁定卡和轻量场景锁定卡
- 角色参考强度分为：高锚点参考型 / 中度参考型 / 低参考原创转译型
- 风格表达使用通用的“高品质 3D 动画电影质感”，避免直接绑定特定品牌

## 参考资料

- `references/output-contract.md`：设计输出形态、参考强度和落盘约定
- `docs/风险与版权边界.md`：参考强度与风险边界
