# SceneForge Web Console 阶段执行问题交接

生成时间：2026-06-16 15:05:38 Asia/Shanghai

## 交接目标

下一次会话继续诊断并修复 SceneForge Web Console / SOP 管线阶段执行问题。当前问题仍未完全解决，不要从“已修复”假设继续。

用户当前反馈：点击“继续执行”后，界面仍有异常。截图中 assistant 文案重复，并出现阶段叙述混乱：先说 `story` 已完成、下一步进入 `assets`，又显示“当前阶段 story 已完成，assets 已启动”，随后读取 `PROJECT_STATE.json`，但流程没有稳定进入 assets 阶段产物生成。

## 当前真实项目状态

当前测试项目：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/projects/test061603`

最后一次本地 CLI 状态查询：

```bash
cd /Users/tangwujun/Documents/trae_projects/scene_forge/projects/test061603
node ../../packages/engine/dist/cli.js status --json
```

结果要点：

- `current_stage: assets`
- `topic_gate.status: completed`
- `reference.status: completed`
- `story.status: completed`
- `assets.status: in_progress`
- `design` 及后续阶段仍是 `ready`

因此，CLI 状态机角度已经进入 assets；但 Web Console 对话执行仍表现不稳定，疑似 stage session / 轻量上下文 / UI 历史展示 / runClaude 阶段切换之间仍有问题。

## 已做过的重要修复

这些改动尚未提交，工作区很脏。不要回滚用户或前序改动。

### Web Console 阶段上下文与权限

相关文件：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/server.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/claudeArgs.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/stageLightGuard.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/stageState.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/tests/stage_light_guard.test.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/tests/stage_state.test.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/server/tests/claude_args.test.ts`

已处理过：

- 增加 CLI stage id 与 scene skill name 的映射，例如 `scene-topic-gate -> topic_gate`。
- stage_light 上下文现在注入当前项目目录、当前 CLI stage、对应 scene skill stage。
- stage_light resume 也会注入最新上下文，避免旧会话完全忽略新规则。
- 移除服务端自动 start 当前阶段的逻辑，避免新项目一进来就被自动置为 `in_progress`。
- guard 已允许当前项目目录内读写、允许合法 engine CLI 命令，例如：

```bash
cd "<projectPath>" && node ../../packages/engine/dist/cli.js complete --stage story 2>&1 | head -20
```

### UI 状态源

相关文件：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/src/App.tsx`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/apps/web-console/src/VariantB.tsx`

已处理过：

- 当前阶段显示优先使用 `PROJECT_STATE.json`，再 fallback 到 board routing。
- 左侧阶段状态显示优先使用 CLI / PROJECT_STATE 状态，避免 `PROJECT_BOARD.md stage_index` 误导。

### Engine 校验与状态同步

相关文件：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/cli.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/state_machine.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/artifact_sync.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/validators/validator.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/validators/schemas.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/tests/cli.test.ts`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/packages/engine/src/tests/validator.test.ts`

已处理过：

- `validate --stage` 会先 `syncStageArtifactIndex()`，让磁盘产物自动注册。
- `validate --stage` 现在会写回 `PROJECT_STATE.json`：
  - 通过：阶段状态变为 `validated`
  - 失败：阶段状态变为 `review_failed`
- `complete --stage` 现在接受 `validated` 状态，否则会出现“validate 通过后 complete 反而不能执行”。
- `artifact_sync` 不再在普通 sync/validate 时把 board stage_index 强行写成 `completed`。
- story validator 已能识别 YAML beat id，例如 `id: beat_1`，不再只认 `beat_id:`。
- topic gate schema 放宽，接受实际 YAML patch 格式。

### Skill 协议调整

相关文件很多，重点是：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-topic-gate/SKILL.md`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-topic-gate/references/output-contract.md`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-reference-decider/SKILL.md`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-reference-decider/references/output-contract.md`

已处理过：

- topic gate 不再要求 `outputs/topic.md` 必须包含旧的 `## topic_ideas` 标题。
- reference decider 已按用户要求放宽：从真人/影视转动画、3D、漫画等显著风格转译时，不应一刀切禁止继承镜头功能、动作节奏、关键姿态、服装轮廓锚点，但必须避免无转译照搬演员身份、品牌元素、完整可识别镜头组合等。

## 已验证过的命令

这些命令最近跑通过：

```bash
pnpm --filter @scene-forge/engine build
node --test dist/tests/cli.test.js dist/tests/state.test.js dist/tests/validator.test.js
node --test apps/web-console/dist/server/tests/stage_light_guard.test.js apps/web-console/dist/server/tests/stage_state.test.js apps/web-console/dist/server/tests/claude_args.test.js
git diff --check
```

并且对真实项目跑过：

```bash
cd /Users/tangwujun/Documents/trae_projects/scene_forge/projects/test061603
node ../../packages/engine/dist/cli.js validate --stage story --json
```

该命令已通过，并随后 story 成功 completed，assets 已 started。

## 当前仍未解决的问题

### P0：Web Console 继续执行仍不稳定

用户最新截图显示：

- assistant 输出重复句子。
- “当前阶段”叙述重复、格式异常。
- 说 story 已完成并 assets 已启动后，又只是读取 `PROJECT_STATE.json`，没有稳定进入 assets 阶段处理。
- 用户认为“当前阶段还是有报错”，需要下一轮继续诊断。

建议不要再只看 CLI。下一轮应建立 Web Console 层面的反馈循环，最好直接重放 WebSocket / runClaude 调用路径，确认：

- `runClaude()` 每次点击“继续执行”时拿到的 `stage` 是否已经从 `story` 变为 `assets`。
- `getStageSession(targetPath, stage)` 是否切换到 assets 独立 session。
- `isFirstMessage` 是否因为旧 jsonl 存在而 resume 了污染过的旧会话。
- `buildProjectContext(targetPath, 'stage_light')` 注入的 stage 是否是 assets。
- 前端显示的是当前 run 的输出，还是旧 session history 与新输出混合。
- stage_light 被 guard 中断后创建 fresh session 的逻辑是否覆盖所有失败路径。

### P0：stage session 可能没有在阶段推进后强制轮转

代码已有按 stage 保存 session 的机制：

- `.scene_forge_session_meta.json`
- `getStageSession(projectPath, stage)`
- `createStageSession(projectPath, stage)`

但截图现象像是旧阶段上下文仍参与了新阶段执行，尤其是输出里重复出现“story 已完成，assets 已启动”，但后续动作没有进入 assets 产物生成。

下一轮重点检查：

- stage 从 `story` complete 后变成 `assets in_progress`，WebSocket 当前连接里的 `sessionId/isFirstMessage` 是否马上刷新。
- 当前连接初始化时的 `targetPath` 是否固定为旧 activeProjectPath。
- FileWatcher 或 `/api/projects` 推送后，前端是否重新请求/切换了 session。

### P1：assets 阶段协议与 validator 是否真的打通

当前已经进入 `assets in_progress`，但还没验证 assets 阶段能否顺利产物生成与 complete。

下一轮需要读取：

- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-asset-checker/SKILL.md`
- `/Users/tangwujun/Documents/trae_projects/scene_forge/.agents/skills/scene-asset-checker/references/output-contract.md`
- `node ../../packages/engine/dist/cli.js rules --stage assets`
- 当前项目 `PROJECT_STATE.json`
- 当前项目 `PROJECT_BOARD.md`
- 当前项目已有 `details/story/story_development_v1.md`
- 当前项目已有 `details/reference/reference_boundary_v1.md`

确认 assets 应读上游产物、应写哪些文件、validator 期待哪些 marker。

## 推荐下一步诊断路径

1. 建立最小反馈循环：

```bash
cd /Users/tangwujun/Documents/trae_projects/scene_forge/projects/test061603
node ../../packages/engine/dist/cli.js status --json
node ../../packages/engine/dist/cli.js rules --stage assets --json
```

2. 单独验证 engine assets validator，不通过就先修 engine/skill 协议：

```bash
cd /Users/tangwujun/Documents/trae_projects/scene_forge/projects/test061603
node ../../packages/engine/dist/cli.js validate --stage assets --json
```

3. 再看 Web Console runClaude 层：

- 给 `runClaude()` 加临时唯一前缀日志，例如 `[DEBUG-stage-session]`。
- 记录 `stage`、`sessionId`、`isFirstMessage`、`jsonlPath`、`ctx` 里的 CLI stage。
- 复现一次点击“继续执行”。
- 修完后移除调试日志。

4. 若确认是旧 stage session 污染：

- 阶段推进后强制 `createStageSession(projectPath, nextStage)`。
- 或在 `getCurrentSopStage()` 变化时，不 resume 上一阶段 session。
- 前端收到 `stage_session` 事件时清理/切换 history，而不是混合旧输出。

## 重要注意事项

- 用户很介意“每个阶段都要点确认”。只有真正需要业务确认时才停；ready / in_progress 阶段不应反复问“是否进入阶段”。
- 当前项目目录 `/projects/test061603` 下所有文件和子目录都应该允许读写。
- 不要读取其他 `projects/*` 历史项目当参考。
- 不要直接手改 `PROJECT_STATE.json` 推进状态；状态推进以 CLI 为准。
- 修复后必须重启 Web Console 服务，否则运行中服务可能仍使用旧 dist / 旧会话。
- 不要宣称问题已解决，除非完成一次 Web Console 层面的真实复现闭环。

## 建议技能

- `diagnose`：必须使用。当前是反复失败的疑难 bug，需要复现、假设、埋点、回归测试。
- `karpathy-guidelines`：代码修改前使用，控制改动范围，避免继续扩大问题面。
- `scene-forge`：理解当前 SceneForge 项目路由和 SOP 上下文。
- `scene-asset-checker`：下一阶段已进入 assets，需要核对资产检查阶段协议。

## 交接结论

当前 CLI 层的 `story -> assets` 状态推进已经发生：`story completed`，`assets in_progress`。但 Web Console 会话执行仍未稳定进入 assets 阶段产物生成，最新截图仍有重复输出和阶段叙述异常。下一轮应优先诊断 WebSocket/runClaude/stage session/history 混合问题，而不是继续只改 validator。
